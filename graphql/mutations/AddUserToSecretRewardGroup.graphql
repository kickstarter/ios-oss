mutation addUserToSecretRewardGroup($input: AddUserToSecretRewardGroupInput!) {
  addUserToSecretRewardGroup(input: $input) {
    project {
      id
      rewards{
        nodes {
          id
          name
        }
      }
    }
  }
}
