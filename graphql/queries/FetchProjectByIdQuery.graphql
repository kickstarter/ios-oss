query FetchProjectById($projectId: Int!) {
  me {
    chosenCurrency
  }
  project(pid: $projectId) {
    ...ProjectFragment
    storyRichText {
      ...RichTextComponentFragment
    }
    backing {
      id
    }
    flagging {
      id
      kind
    }
  }
}
