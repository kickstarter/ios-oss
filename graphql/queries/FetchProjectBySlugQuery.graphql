query FetchProjectBySlug($slug: String!) {
  me {
    chosenCurrency
  }
  project(slug: $slug) {
    ...ProjectFragment
    storyRichText {
      ...RichTextComponentFragment
    }
    backing {
      id
    }
    flagging {
      id
      kind
    }
  }
}
