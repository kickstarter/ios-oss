# Query used to fetch the backing for the Manage Pledge page.
query FetchBackingWithIncrementsRefunded($id: ID!, $includeShippingRules: Boolean!, $includeLocalPickup: Boolean!) {
  backing(id: $id) {
    addOns {
      nodes {
        ...RewardFragment
      }
    }
    ...BackingFragment
    reward {
      ...SimpleShippingRulesExpandedFragment
    }
    paymentIncrements {
      ...PaymentIncrementBackingFragment
    }
  }
}
