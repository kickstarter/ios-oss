query RichTextExampleProjectsQuery($storyRichText: Boolean!){
  projects(recommended: true, first: 10) {
    nodes{
      id
      name
      storyRichText {
        ...RichTextComponentFragment
      }
    }
  }
}

query RichTextExampleProjectBySlugQuery($slug: String!, $storyRichText: Boolean!){
  project(slug: $slug) {
    id
    name
    storyRichText {
      ...RichTextComponentFragment
    }
  }
}

