query RichTextExampleProjectsQuery($storyRichText: Boolean = false){
  projects(recommended: true, first: 10) {
    nodes{
      id
      name
      storyRichText {
        ...RichTextComponentFragment
      }
    }
  }
}

query RichTextExampleProjectBySlugQuery($slug: String!, $storyRichText: Boolean = false){
  project(slug: $slug) {
    id
    name
    storyRichText {
      ...RichTextComponentFragment
    }
  }
}

