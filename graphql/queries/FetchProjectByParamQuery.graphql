query FetchProjectByParam($projectId: Int, $slug: String) {
  me {
    chosenCurrency
  }
  project(pid: $projectId, slug: $slug) {
    ...ProjectFragment
    backing {
      id
    }
    flagging {
      id
      kind
    }
  }
}
