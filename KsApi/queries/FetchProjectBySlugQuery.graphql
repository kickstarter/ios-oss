query FetchProjectBySlug($slug: String!, $withStoredCards: Boolean!, $includeShippingRules: Boolean!) {
  me {
    chosenCurrency
  }
  project(slug: $slug) {
    ...ProjectFragment
    addOns {
      nodes {
        ...RewardFragment
      }
    }
    backing {
      id
    }
  }
}
