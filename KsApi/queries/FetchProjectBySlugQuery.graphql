query FetchProjectBySlug($slug: String!, $withStoredCards: Boolean!) {
  project(slug: $slug) {
    ...ProjectFragment
    rewards {
      nodes {
        ...RewardFragment
      }
    }
    addOns {
      nodes {
        ...RewardFragment
      }
    }
  }
}
